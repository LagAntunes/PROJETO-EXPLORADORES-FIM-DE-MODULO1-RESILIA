// início da programação da primeira fase do indiana //
function escolha(){
    alert ("Escolha e digite no prompt alguma das alternativas A ou B (em maiúsculo e somente uma opção)");
    var destino = prompt("Escolha seu destino");
    if (destino == 'A') {
        return location = ('../personagens.html');
    } else if (destino == 'B') {
        return location = ('../index.html');
    } else {
        return escolha()
    }
}
// fazer uma página jvs pra cada fase***************************


// fim da programação da primeira fase do indiana //